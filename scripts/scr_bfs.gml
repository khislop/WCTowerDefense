var mapArray = argument0;

var someList = ds_list_create();
var queue = ds_queue_create();

var root_parent = null;
ds_queue_enqueue(queue,array[0, 0]);

while (ds_queue_empty(queue)) {
var current = ds_queue_dequeue(queue);
    if current = goal {
//        return some path;
    }
    
}

return path;
